// Variables and types
long max = 9000;
byte level = 42;
flag = true;
nums = [1, 2, 3];
user = {name: "Ada", active: true, scores: nums};

// Function with reference
func inc(&value):
    value += 1;
endfunc

// Conditional
if(flag && max > 100):
    print("User: " + user.name + " level " + level);
    inc(max);
else:
    print("Disabled");
endif
