// Variables and types
long max = 843509834;
byte level = 42;
flag = true;
nums = [1, 2, max];
user = {name: "Ada", active: max};

print(nums[2]);

// Function with reference
func inc(&value):
    value += 1;
endfunc

// Conditional
if(flag && max > 100):
    print("User: " + user.active + " level " + level);
    inc(max);
else:
    print("Disabled");
endif
